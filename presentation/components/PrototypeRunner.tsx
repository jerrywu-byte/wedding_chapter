"use client";import{useWeddingStory}from"../shared/use-wedding-story";import{experienceMeta,type ExperienceId}from"../shared/experience-registry";import type{PrototypeScreen}from"../shared/experience-adapter";import"../shared/accessibility.css";
export default function PrototypeRunner({experienceId="handmade-vows",forcedScreen,embedded=false}:{experienceId?:ExperienceId;forcedScreen?:PrototypeScreen;embedded?:boolean}){const story=useWeddingStory(forcedScreen??"opening");const Experience=experienceMeta[experienceId].component;return <Experience screen={forcedScreen??story.screen} selectedOptionId={story.selectedOptionId} onSelect={story.setSelectedOptionId} onNavigate={story.setScreen} embedded={embedded}/>}
