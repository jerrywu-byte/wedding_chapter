import React from"react";import{renderToStaticMarkup}from"react-dom/server";import fs from"node:fs";import path from"node:path";import{experienceIds,experienceMeta}from"../presentation/shared/experience-registry";import{previewQuestion,type PrototypeScreen}from"../presentation/shared/experience-adapter";
const screens:PrototypeScreen[]=["opening","chapter","result"];const root="/workspace/scratch/wedding-chapter-screens";fs.mkdirSync(root,{recursive:true});
for(const id of experienceIds){const css=fs.readFileSync(path.join(process.cwd(),`presentation/experiences/${id}/experience.css`),"utf8")+fs.readFileSync(path.join(process.cwd(),"presentation/shared/accessibility.css"),"utf8");const Experience=experienceMeta[id].component;for(const screen of screens){const markup=renderToStaticMarkup(<Experience screen={screen} selectedOptionId={previewQuestion.options[0].optionId} onSelect={()=>{}} onNavigate={()=>{}} embedded/>);const html=`<!doctype html><html lang="zh-Hant"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><style>*{box-sizing:border-box}html,body{margin:0}button{font:inherit}${css}</style></head><body>${markup}</body></html>`;fs.writeFileSync(path.join(root,`${id}-${screen}.html`),html)}}
console.log(root);
